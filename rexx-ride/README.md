# Rexx Ride 3D

Browser game ready for Netlify.

## Netlify
- Base directory: `rexx-ride`
- Build command: leave blank
- Publish directory: `.`

`index.html` opens the 3D game automatically.

The game uses Three.js from a public CDN, so the browser needs internet access to load the 3D engine.